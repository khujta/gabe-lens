# Plan — the four entity models: claim · seeded · derived · proposed (2026-09-06)

**Status:** EXECUTED 2026-09-06 — Phases 0–4 shipped (`fbe434b` · `b2e16c5` · `abd0f47`/`3495c74` · `993a62d` · `bf35c02`), Phase 5 (docs · install · doctor · estates) in flight; the design record is [README.md](README.md). Original header follows. Operator decisions 2026-09-06 (this session): build all three models, switch with a pill (R2–R4 dropped); table anchors + adaptive-depth URL-domain names; proposed = the map as if accepted (its own view) + `entities.draft.json`; naming descends only while a domain's atoms name >1 anchor table (DEPTH_CAP 3 — the measured rule; the `>N endpoints` clause dropped). Reconciled from three read-only designs (A emitters · B station · C tools/pulse/drafter/docs) by one judge. Ground: suite `1d3854e` (branch `graft-adoption`, clean); the six estates (gustify · gastify · tier0–3). Suggested home: `docs/design/entity-models/plan.md` beside the design record this pass creates.

**Operator decisions held FIXED:** four pill positions claim · seeded · derived · proposed · table anchors + adaptive-depth URL-domain names · proposed = the map AS IF accepted (its own 4th view) AND a human-facing `entities.draft.json` from a read-only drafter · R2–R4 wire-view rows dropped (R1 already retired).

**The one law the whole pass hangs on (goes into CLAUDE.md Conventions):** `claim` is the REGISTRY and the JOIN KEY — every tool, hook, generator and station registry joins on the claim slug; `seeded` / `derived` / `proposed` are VIEWS resolved per node as `home = (C4.models.homes[view] || {})[id] || (LEVELS.models.homes[view] || {})[id] || entClaim`, and nothing ever joins on their names.

Every anchor below was read at `1d3854e`; line numbers orient — **the builder anchors on the literal with an assert-count-1 replacement, never on the line.** Every station edit lands in BOTH `templates/center/shell/gabe-universe.html` and `templates/center/shell/example/codebase-graph-station/gabe-universe.html` (they differ only at L1, L943-944, L957-958, L962, L964, L970, L972, L998, L1014-1015 — placeholders above the first anchor). R10: `unclaimed` · `detached` · `candidate` · `aspect` · `layer` — never "orphan".

---

## Part A — emitters + data (the machine half)

### A1 · The ungated element census (prerequisite 1)
- **File:** `templates/center/generators/_a3_code.py` — anchor `:971 def file_census(repo: Path, entity_code: dict | None = None) -> dict:` · the non-recursive gate `:952` / `:1011 for py in sorted(dp.glob("*.py")):` · caches `:835 _ROUTE_CENSUS` / `:836 _FILE_CENSUS`.
- **Change:** new `element_census(repo, entity_code=None) -> dict`, cached as `_ELEMENT_CENSUS` on the sibling pattern. BOUND = the CLAIM ROOTS (the literal prefix of each `code.<layer>` glob before the first `*?[`, collapsed to shallowest ancestors), walked RECURSIVELY (`rglob`), with the existing skip idioms (`_MOUNT_SKIP :252`, `_CENSUS_SKIP :920`, `test_`/`.test.`). Each UNCLAIMED `.py` is `_safe_parse`d once → `{file, lang:'py', fns:[names], tables:[names], routes:N, lines, reason}`; `.ts/.tsx` are NOT parsed — the unpieced files are a set difference over `fe.pieces[].file` (the fe arm already globs the whole root) and are COUNTED + NAMED, never minted (a file the compiler found no export in is config/test/types mass). Emit `amap['element_census'] = {scanned_roots, claimed:{py,ts}, elements:[unclaimed py only], ts_unpieced:[files], stats}`; archmap `version` 2→3 at `build_center_a3.py:1968`. **`file_census` / `route_census` stay byte-identical** (pulse S13 + the cc-init adopt rail read their exact rows); both docstrings gain one line naming the other census's bound so the two are never cited against each other.
- **Battery:** NEW `tests/entity-models/run.sh` (auto-discovered by `scripts/suite-doctor.sh:98`). FIRE: synthetic tree, `pkg/api/a.py` claimed, `pkg/api/sub/b.py` unclaimed → `b.py` listed with its fn names. SILENT: everything claimed → key ABSENT (the P5 rule; never `{elements: []}`); a bare `__init__.py` never listed. MUTATION LEVER: `rglob`→`glob` reddens FIRE. Determinism: two runs byte-identical.
- **Cost:** MEASURED +20 files/19 defs gustify · +58/254 gastify · +11/13 tier0 · +7/0 tier1 · +2/0 tier2 · +1/0 onyx (99 files / 286 defs total; the wider top-package bound was rejected — onyx +325 files of alembic/scripts noise).

### A2 · Element nodes minted into the CLAIM map's `__unclaimed__`
- **File:** `templates/center/generators/_a3_graph.py` — anchor `:62 _UNCLAIMED = "__unclaimed__"` · the middleware mint precedent `:1301` · `:1370 return fold_fe({`.
- **Change:** when `amap['element_census']` is present, mint ONE l2 node per python element into `l2.__unclaimed__`: `{id:'element:<file>', kind:'element', label:<basename>, fns, tables, routes, lines, det:{PURPOSE:'a file no entity claims', file}}`, no edges (its fns are levels nodes; its routes ride `route_census` already). `stats.elements = {present, files, fns, routes, ts_unpieced}`. The operator's words are "unclaimed ones under `__unclaimed__`" — this is the map saying it, not a side table. Census absent → l2 byte-identical (the arm's own presence flag).
- **Battery:** `tests/arch-graph`. FIRE: a census with one element → one `element:` node in `l2.__unclaimed__` and `stats.elements.present`. SILENT/BYTE-IDENTITY: census absent → `json.dumps` of l1/l2/cross_edges/fe identical to today. MUTATION LEVER: home the element to the first declared slug — the FIRE assert on `__unclaimed__` reddens. Node-budget assert: element count ≤ `ELEMENT_CAP` (2000), `truncated:true` when clipped.
- **Cost:** +58 nodes gastify, +20 gustify (example page), +1 onyx — under the 1,600 budget everywhere it is not already folded.

### A3 · `_a3_models.py` — the union dataflow graph + the SHARED/HUB class (prerequisite 2)
- **File:** NEW `templates/center/generators/_a3_models.py` (its own module — `_a3_graph.py` is 1,476 lines, budget 800). Reads the shapes at `_a3_graph.py:1370` and `_a3_levels.py:280 _fedges.append({"s": c["s"], "ss": c["ss"], ...})`.
- **Change:** `_dataflow(amap, graph, levels)` → directed consumer→produced edges, weighted: `levels.fn_edges` calls/depends/dispatches/reaches w=1 (graft — a FLOOR) · `fe.edges` renders/uses-hook/uses-store/imports/fecall/typed w=1 (compiler-proven) · `cross_edges kind:'bridge'` w=2 · l2 touches/consumes/nests/serializes/reads_from w=1 · `writes_to` + `access.ops rw=='w'` **w=3** (a write is ownership — the operator's bound). `hub_class(id)` from idioms already on the maps — **gate**: named as `endpoints[*].middleware[].fn|.callee` with `gate:true` across ≥2 entities (`_a3_code.py:1863 _is_mw_gate`) · **api-client**: fe `mclass=='api'` or an fe module `fecall`-reached from ≥3 homes · **platform**: home is `fe.homes[].kind=='bucket'` or `__unclaimed__` · else None. `HUB ⇔ |homes(consumers)| ≥ HUB_HOMES(3) ∧ indeg ≥ HUB_FLOOR[class]` with floors `{gate:1, api-client:1, platform:8, default:20}`. Emits `models.shared[]{id,class,homes,indeg,domains?}` sorted by (class,id), capped, `truncated` named. Every 'shared' Part C verdict is a hub by construction (it never carries `move`).
- **Battery:** `tests/entity-models`. FIRE ×3: default-floor hub (4 homes, indeg 30, no class) · api-client hub (3 homes, indeg 2) · gate hub (on endpoints of 3 entities, indeg 1). SILENT: 2 homes at indeg 200 is NOT a hub (the homes bar, not degree). MUTATION LEVER: every floor → 1 reddens the SILENT case. **Kill condition for `platform:8`** (stated in the module header, the pulse-angle discipline): if a regen on any estate selects 0 or >10% of fe pieces as platform hubs, the floor is wrong — say it on `views.seeded.note`, do not tune silently.
- **Cost:** O(E) ≤ 8.5k edges on onyx. MEASURED hubs: fe 33/1,077 · 14/410 · 11/346 · 48/2,602; backend 2 · 0 · 0 · 8 (`cx.ts`, `Button`, `useT`, `lib/api.ts`, `get_auth_context`, `require_permission`, `get_session_with_current_tenant` …).

### A4 · SEEDED (P1) — Part C move band, hubs held, tier-consistent targets only
- **File:** `_a3_models.py` · anchor `_a3_homing.py:75 def verdict(home, users, data)` · `:121 "to_kind": (("entity" if to in ents else "fe-area") if to else None)` · `:200 extra["data_note"]`.
- **Change:** `seed(hom, hubs) -> (homes, held, abstain)`: `homes[id]=to` for every Part C piece with `verdict=='move'` AND `id not in hubs` AND the target is TIER-CONSISTENT — a backend piece moves only to a declared entity (`to_kind=='entity'`); a frontend piece moves only to an `fe·<slug>` home (never to a bucket: `design-system`/`app-shell`/`__unclaimed__` MAY CONSUME, NEVER RECEIVE — the `_a3_homing.py:9` law extended). `held[]` = move verdicts held by the hub class; `abstain[]` = move verdicts refused by the tier rule (named, never silently dropped). Copy `_a3_homing.rule` verbatim into `models.bands.seeded`. `views.seeded = {present, moved, held, abstained, weighed, note}` — the note STATES the measured rate so nobody reads the pill as the analysis's 34–42% free propagation.
- **Battery:** `tests/entity-models`. FIRE: move→B, not a hub, tier-consistent → `homes[id]=='B'`. SILENT: same piece marked HUB → absent, counted in `held`. SILENT: a backend piece whose `to` is `fe·x` → absent, counted in `abstain`. SILENT: stay/shared/agree never appear. MUTATION LEVERS: drop the hub clause (held case reddens) · drop the tier clause (abstain case reddens). Honest-empty: `hom.present==False` → `views.seeded={present:false, reason:<byte-copied>}` and NO `homes.seeded` key.
- **Cost:** MEASURED moves→after hold-out 20→17 gustify · 7→7 gastify · 5→4 tier0 · 2→2 · 1→1 · 244→239 onyx (2.0% · 1.7% · 2.5% · 7.8% of weighed).

### A5 · DERIVED (P2) — request atoms → write-majority table → adaptive-depth domain name
- **File:** `_a3_models.py` · anchors `_a3_graph.py:529 def _l2(` (endpoint nodes carry `access`) · `skills/gabe-pulse/scripts/entity_shape.py:32 def url_domain(path: str) -> str:` (the `PARAM` idiom, reused not copied).
- **Change:** (a) ATOMS — one per l2 endpoint node (BOOT excluded; TASK carried `via:'task'`): `{ep, method, path, claim_slug, schemas[], fetchers[] (bridge `export` ids), anchor}`; anchor = write-majority table of `access.ops rw=='w'`, else read-majority, tie → sorted-first. (b) MERGE — one cluster per anchor table. (c) NAME — non-parameter path-segment tree; DESCEND from depth 1 while the node's atoms name >1 anchor table; stop at a singleton or `DEPTH_CAP 3` (`truncated` when the cap clipped); cluster name = majority leaf domain → tie: the anchor TABLE name → then the top screen (most bridges); `center.config.url_domain_map` renames a naked segment; `named_by ∈ domain|table|screen` + `depth` carried per row (the `>N endpoints` clause is the one open operator decision — see the end). (d) MEMBERS — endpoint + schemas + fetching exports + the non-hub fns in its `behind` tree not claimed by another anchor + census elements whose file defines/writes the anchor table (levels-side ids ride the levels slice, A8). (e) RESIDUE — `aspect` rows: gate fan-in ≥3 URL domains (→ the gate fn(s) MOVE to `a:<gate>`, a small dashed hull at the functions tier) · write fan-in ≥3 declared entities on one table (REPORT row, the model node stays — it is a feature's anchor) · fe pieces consumed from ≥3 homes (REPORT row, members capped, pieces HELD — moving 84/22/142 pieces into one hull would erase the geography); `layer` rows: a cluster with zero endpoints AND zero tables (REPORT only this pass, fns keep claim). Depends-modularity (Q<−0.2) is DEFERRED — the other three fire cleanly on all four estates and it needs a partition the view never computes; trigger: an aspect the three detectors miss on a twin. (f) ABSTAIN — an atom with no anchor keeps its claim, listed in `abstain.derived` and counted in `views.derived.abstained`. (g) Cluster ids: backend `d:<anchor_table>`; a feature with fe members gets the TWIN `fe·d:<anchor_table>` with `pair:'d:<anchor_table>'` — the claim's `fe·<slug>` law reused verbatim so `__uniIsFeEnt` (`gabe-universe.html:3365`), fleet split and pair seating need no change. `color` per BACKEND cluster id via `_a3_render.entity_color` (`_a3_render.py:136`); twins take the station's lerp-from-pair (`:1242`). Roster rows carry `claim_mix`, `purity` (majority share vs claim) and `why`.
- **Battery:** `tests/entity-models`. FIRE: two endpoints writing T under /x/a and /x/b → ONE feature `x` anchored T. FIRE: T reached from /x and /y with a second anchor under /x → descends to `x/a` + `y`. FIRE: a gate on endpoints of 3 domains → `aspect` row + `a:<gate>` home on the levels side. FIRE: an fe fetcher joins → `fe·d:T` twin with `pair`. SILENT: endpoint with no access ops → no home, `abstained`+1 (never guessed into a domain). SILENT: descent stops at `DEPTH_CAP` with `truncated`. MUTATION LEVER: swap w=3 writes to w=1 — the anchor flips on the write-vs-read fixture and the naming assert reddens. Determinism: features sorted by (name, anchor).
- **Cost:** MEASURED anchoring 78/80 gustify · 49/49 gastify · 327/499 onyx (172 abstain, 34%); clusters 27 · 11 · 66; PURITY vs declared **0.910 · 0.898 · 0.893** (the unsupervised LPA on the same estates: 0.646 · 0.654 · 0.576) — deterministic, no LPA, no seed.

### A6 · PROPOSED (P3) — one verdict per declared entity + candidates, the map AS IF accepted
- **File:** `_a3_models.py` · anchor `entity_shape.py:40 def entity_shape(endpoints, url_domain_map=None)` (IMPORTED for the aspect/sole-owns rule — no reimplementation).
- **Change:** `propose(derived, amap, shape) -> (roster, homes)`; precedence ASPECT > LAYER > SPLIT > MERGE > FEATURE, each row `{slug, verdict, why, evidence, suggested_edit}`: FEATURE = majority slug of ≥1 derived feature AND sole-owns ≥1 URL domain · SPLIT = atoms in ≥2 features each ≥ `SPLIT_MIN 3` with distinct anchors AND distinct leaf domains · MERGE = the atoms' majority feature's majority slug is ANOTHER entity; symmetric pairs deduped; **survivor = the entity with more atoms in that feature → tie: the one that sole-owns a URL domain → tie: sorted-first** (named in `why`; the draft carries both so a human can flip it) · ASPECT = `entity_shape`'s rule (co-claims ≥3, sole-owns ≤1 — fires today on onyx `auth`) OR homes a gate with fan-in ≥3 domains · LAYER = no endpoints, no tables, files consumed by ≥3 entities. An entity with zero atoms → `verdict:null, why:'no endpoint, no table — nothing to weigh'`. CANDIDATES = a derived feature whose majority slug is `__unclaimed__`, or spanning ≥3 declared entities with no majority (gustify `dish_history_events`: recipe 5 · cooking 1 · pantry 1 · progression 1). `homes.proposed` = AS IF ACCEPTED: FEATURE → nothing; SPLIT → each atom + its schemas/fetchers/behind-fns → `d:<table>` (fe → `fe·d:<table>`); MERGE → both sides → the survivor slug (fe → `fe·<survivor>`); ASPECT/LAYER → **nothing moves** (the roster marks the kind, the station tints the card — a layer that teleports its pieces is unreadable); candidates → their `d:` id (elements included).
- **Battery:** `tests/entity-models`. FIRE: 5/4 split across two anchors+domains → SPLIT naming both ids. FIRE: two entities majority in one feature → ONE MERGE row, survivor by atom count. FIRE: entity homing a gate across 4 domains → ASPECT and zero entries in `homes.proposed`. SILENT: a clean entity → FEATURE, zero delta entries. MUTATION LEVERS: let ASPECT move pieces (reddens) · flip survivor to sorted-first (the atom-count fixture reddens). Roster sorted by slug, candidates by name.
- **Cost:** `entity_shape` already runs ≈0.3 s per estate read-only.

### A7 · The c4 `models` block + `stats.models` — DELTA homes, byte-identical when stripped
- **File:** `_a3_models.attach(graph, mod)` · anchors `_a3_homing.py:225 def attach(graph, ev)` (the precedent) · `_a3_graph.py:1459 def emit(`.
- **Change:** ONE new top-level key `graph['models']` + `graph['stats']['models']` (shape in the data section). `homes[view]` are DELTA maps (only ids whose home ≠ claim); `abstain[view]` and `held[view]` are id LISTS (B's point: "did not move" and "could not be judged" are two facts on the page). NO per-node `home_*` fields (3,396 nodes on onyx; breaks the strip test). `mod.present==False` → `stats.models={present:false, reason}` and NO `models` key (the graft/web pattern at `_a3_graph.py:1425-1450`).
- **Battery:** `tests/arch-graph`. FIRE: `models.views.claim.present` and `stats.models` set. BYTE-IDENTITY: `strip(graph_with) == graph_without` on `json.dumps`. MUTATION LEVER: write `home_seeded` onto one l2 node → reddens. Cap assert: every delta ≤ node count; every id in `homes.*` resolves to an l2/fe id (c4 side) — never a levels id.
- **Cost:** MEASURED full-map alternative rejected (+331K/+104K/+713K — onyx +23%); the delta costs 17/7/239 seeded entries + derived/proposed deltas.

### A8 · The levels.json mirror slice + the emit reorder
- **File:** `templates/center/generators/_a3_levels.py:670 def emit(` + `build_center_a3.py:2161 _levels["homing"] = _hom` / `:2162 _a3_levels.emit(_levels, CENTER_OUT)`.
- **Change:** backend FUNCTIONS are levels nodes (the station mints them at `gabe-universe.html:2009 _buildFnData` from `GABE_LEVELS.fn_nodes`; gabe-map reads levels lazily) — their homes CANNOT ride c4. `_a3_models.levels_slice(mod)` → `levels['models'] = {head, views (forwarded), homes:{seeded,derived,proposed} restricted to `<file>#<fn>` ids, abstain, held, bands}`. MOVE the levels emit to AFTER the models build (A9); the two files agree on `models.head`.
- **Battery:** `tests/levels`. FIRE: a seeded fn move appears in `levels.models.homes.seeded` and NOT in the c4 delta. SILENT: no models → `levels.models` absent, bytes identical. DISJOINTNESS: no id in both maps. MUTATION LEVER: restore the old emit position → FIRE reads a file written before the slice and reddens.
- **Cost:** minified levels (`_a3_levels.py:673`), ≤239 entries worst case.

### A9 · `build_center_a3.py` wiring — own try/except, own presence flag, loud degrade
- **File:** `build_center_a3.py` — anchors `:42 import _a3_homing` · `:1998-2007` (route/file census assembly) · `:1968 "version": 2` · `:2159 _a3_homing.attach(_graph, _hom)` · `:2170 _a3_graph.emit(_graph, CENTER_OUT)` · the arm pattern `:2097`/`:2124`.
- **Change:** import `_a3_models` beside `_a3_homing`; call `element_census` beside the two censuses (before the archmap write); bump `version` to 3; between `:2159` and the moved levels emit: `_mod = _a3_models.build(amap, _graph, _levels, hom=_hom)` inside its OWN try/except → `{present:False, reason:'entity-models error: …'}` + a `⚠ entity models SKIPPED (station shows claim only)` line; `_a3_models.attach`; `_levels['models'] = levels_slice(_mod)`; THEN `_a3_levels.emit`; THEN `_a3_graph.emit`. One report line: views present · features · moved · abstained · hubs · elements. The models build COMPLETES before either emit so a partial failure can never leave levels carrying a slice c4 lacks.
- **Battery:** `tests/center`. FIRE: a synthetic project regens with a block and the report line names four views. SILENT: monkeypatch `_a3_models.build` to raise → both files written, `stats.models.present==False`, exit 0. MUTATION LEVER: remove the try/except → SILENT aborts. ORDER: levels.json on disk carries the slice; both heads equal. PROPAGATE: `propagate.sh --check` classifies `_a3_models.py` as `req_gen` (`propagate.sh:37-44`) — the `_a3_homing` twin-break of 2026-09-06 must not repeat; land generator + import in ONE commit.
- **Cost:** ≈0 beyond A1's parses.

### A10 · Data shapes (the exact keys)

**`c4-graph.json` → `models` (+ `stats.models`)**
```jsonc
"models": {
  "head": "<graph.head>",                      // no wallclock, no STAMP
  "rule": "claim = config file claims · seeded = Part C move band, hubs held, tier-consistent · derived = request atoms merged on the write-majority table, named by URL domain at adaptive depth · proposed = one verdict per declared entity, rendered as if accepted. Deterministic: sorted inputs, sorted ties, no LPA, no seed.",
  "default": "claim",
  "views": {
    "claim":    {"present": true, "pieces": 1385, "note": "the config's code.* claims — the map today"},
    "seeded":   {"present": true, "moved": 17, "held": 3, "abstained": 0, "weighed": 859, "note": "Part C move verdicts only — 2.0% of weighed pieces, NOT free propagation"},
    "derived":  {"present": true, "features": 27, "aspects": 3, "layers": 1, "atoms": 80, "anchored": 78, "abstained": 2, "purity": 0.91, "new_clusters": 29, "note": "an unanchored atom KEEPS its claim — no witness, no move"},
    "proposed": {"present": true, "verdicts": {"FEATURE": 5, "SPLIT": 1, "MERGE": 0, "ASPECT": 1, "LAYER": 1}, "candidates": 2, "moved": 23}
  },                                            // an absent view = {"present": false, "reason": "<why>"}
  "bands":  {"seeded": {"move_share": 0.6, "move_min_users": 2, "shared_min": 3, "text": "<verbatim _a3_homing.rule>"}},
  "shared": [{"id": "fe:.../useT.ts#useT", "class": "platform", "homes": 9, "indeg": 228},
             {"id": "apps/api/auth/context.py#get_auth_context", "class": "gate", "homes": 7, "indeg": 78, "domains": 23}],
  "rosters": {
    "derived":  [{"id": "d:cooking_sessions", "twin": "fe·d:cooking_sessions", "name": "cooking", "kind": "feature", "anchor_table": "cooking_sessions",
                  "domain": "cooking", "named_by": "domain", "depth": 1, "color": "#…", "endpoints": 5, "screens": 3, "purity": 1.0,
                  "claim_mix": {"cooking": 5}, "members": ["endpoint:POST /cooking/sessions", "schema:CookingSessionOut", "fe:…#useCookingSession"], "why": "5 endpoints write cooking_sessions under /cooking"},
                 {"id": "a:get_auth_context", "name": "auth context", "kind": "aspect", "detector": "gate-fan-in", "domains": 23, "endpoints": 78, "members": ["apps/api/auth/context.py#get_auth_context"], "drawn": true},
                 {"id": "a:fe-shared", "name": "shared frontend", "kind": "aspect", "detector": "fe-homes", "members": ["…"], "drawn": false, "truncated": true}],
    "proposed": [{"slug": "settings", "verdict": "SPLIT", "why": "…", "evidence": {"atoms": 9, "features": ["d:household_format_preferences", "d:user_exploration_preferences"], "domains": ["me", "settings"]}, "suggested_edit": {"code": {"api": ["apps/api/api/user_settings.py"]}}},
                 {"slug": "auth", "verdict": "ASPECT", "why": "…", "evidence": {"co_claims": ["admin", "auth", "user"], "sole_owns": ["sso"], "gate": "get_auth_context"}}],
    "candidates": [{"id": "d:dish_history_events", "name": "dish history", "kind": "feature", "named_by": "table", "anchor_table": "dish_history_events", "domain": "history", "endpoints": 8, "screens": 2, "spans_entities": ["cooking", "pantry", "progression", "recipe"], "suggested_slug": "dish-history", "color": "#…"}]
  },
  "homes":   {"seeded": {"<c4 id>": "<slug|fe·slug>"}, "derived": {"endpoint:POST /cooking/sessions": "d:cooking_sessions"}, "proposed": {"endpoint:GET /me/format": "d:household_format_preferences"}},
  "held":    {"seeded": ["fe:…#useT"], "derived": ["fe:…"]},
  "abstain": {"seeded": ["<backend piece whose target was fe·x>"], "derived": ["endpoint:GET /health"], "proposed": []},
  "stats": {"caps": {"depth_cap": 3, "hub_homes": 3, "hub_floor": {"gate": 1, "api-client": 1, "platform": 8, "default": 20}, "split_min": 3, "roster_cap": 200, "member_cap": 400, "element_cap": 2000}, "truncated": []}
}
"stats": {"models": {"present": true, "views": ["claim","seeded","derived","proposed"], "features": 27, "shared": 35, "seeded_moved": 17, "derived_abstained": 2, "candidates": 2},
          "elements": {"present": true, "files": 20, "fns": 19, "routes": 0, "ts_unpieced": 249}}
// absent arm → stats.models = {"present": false, "reason": "…"} and NO models key
```
**Namespaces:** l2 ids `endpoint:` · `model:` · `schema:` · `middleware:` · `provider:` · `flag:` · `web:` · NEW `element:<file>`; fe `fe:<file>#<name>`; backend fns `<file>#<fn>` (levels only); clusters `d:<table>` · `fe·d:<table>` · `a:<gate|table|fe-shared>` · `l:<name>` — new prefixes that cannot collide with a slug (the `__unclaimed__` precedent).

**`levels.json` → `models`:** `{head, views, homes:{seeded,derived,proposed} (fn ids only), held, abstain, bands}` — battery asserts the c4/levels key sets are disjoint.

**`archmap.json` (version 3) → `element_census`:** `{scanned_roots[], claimed:{py,ts}, elements:[{file, lang:'py', fns[], tables[], routes, lines, reason}], ts_unpieced:[files], stats:{files, fns, tables, routes, unparseable, ts_unpieced}}` — ABSENT (P5) when no code config or nothing unclaimed.

**`docs/site/center/entities.draft.json`** (A16 writes, cc-init reads; COMMITTED like `workflows.draft.js`):
```jsonc
{"head": "<c4 head>", "generated_from": "c4-graph.models", "rule": "<verbatim>",
 "coverage": {"witnessed": 0.31, "abstained": 2, "note": "31% of declared backend files have a witness — the rest ABSTAIN, the claim stands"},
 "declared":   [{"slug": "settings", "verdict": "SPLIT", "into": ["household format preferences", "user exploration preferences"], "evidence": {…}, "note": "<one sentence in the operator's words>", "suggested_edit": {"code": {"api": ["…"]}}}],
 "candidates": [{"name": "Track dish history — cook · rate · repeat", "kind": "feature", "named_by": "table", "anchor_table": "dish_history_events", "domain": "history", "endpoints": 8, "screens": 2, "spans_entities": ["cooking","pantry","progression","recipe"], "suggested_slug": "dish-history", "members": ["… capped 40"]}],
 "abstained":  [{"scope": "apps/api/integrations/", "reason": "no witness reaches — the claim stands"}],
 "accept": "add or rename entities.<slug> in docs/site/center/center.config.json (code.* claims listed above), then /gabe-cc-init rank",
 "stats": {"declared": 8, "verdicts": {"FEATURE": 5, "SPLIT": 1, "ASPECT": 1, "LAYER": 1}, "candidates": 2}}
```

---

## Part B — the station (both copies)

Verdict carried from Design B, verified: the switch is LIVE, no reboot — `gabe-universe.html:2455 var sa=(SUBANCHOR[n.ent]||{})[n.sub];` reads the home every tick; `buildClusters`/`__uniFleetRender`/`panelAll`/`crossSec` all derive from `nodes` live; colour is kind-driven (`:1221 col:KINDS[kind].col`). A model switch is the same operation the shipped `entLayout` pill performs (`applyCfg :5112`).

### B1 · Stamp the immutable claim at adapter time
- **Anchor:** `:1220 var n={ id:p.id, kind:kind, ent:ent, label:p.label||p.slug||p.id,` · `:1250 var n={ id:p.id, kind:kind, ent:p.home,` · `:2010 _FNNODES=D.fn_nodes.map(function(f){`.
- **Change:** add `entClaim:ent` / `entClaim:p.home` / `entClaim:<fn slug>` to the three node literals (`ent` stays the MUTABLE current home; `entClaim` is the join key for the levels group map, homing evidence and the card's "moved from" row). Also `_ents` gains the ELEMENT kind's registry entry (`KINDS.element`: muted grey planet, layer `code`, tier T2 with functions, legend row "a file no entity claims").
- **Battery:** `tests/gabe-universe` — FIRE: three `entClaim:` literals present; SILENT: `page.count('entClaim:')==3` (nothing assigns it elsewhere). MUTATION: drop one → reddens.

### B2 · Join the levels group map on the claim
- **Anchor:** `:1877 nodes.forEach(function(n){ var pc=D.pieces[n.ent]; if(!pc) return;`.
- **Change:** `D.pieces[n.entClaim||n.ent]` — otherwise every moved piece loses its use-case/community/fk sub-cluster on any non-claim view and lands in "other". The single highest-value line in the pass.
- **Battery:** FIRE: `D.pieces[n.entClaim` present; SILENT: `D.pieces[n.ent]` absent. Headless: switching to seeded does not raise the `sub==='other'` count.

### B3 · The 4-position ENTITY MODEL pill replaces R2–R4
- **Anchor:** `:4082 window.__uniAddWireView=function(){` · `:4087 var DEF=[["cap","CAP","capsules — …` · `:5113 function wireCfg(){` (snapshots `#cfg .pill` BEFORE `__uniAddWireView` runs at `:5136`, so the pill binds its OWN handler, as the CAP/wv buttons do) · call sites `:5042` · `:5136` · `:6125`.
- **Change:** drop the r2/r3/r4 DEF entries (CAP stays alone in `#wireview` — it is the capsule master, not an R row); build `#entmodel` ABOVE it: single-select pill claim · seeded · derived · proposed (the tier-pill grammar at `:3653`), title in the reader's words; a position whose view is `present:false` renders DISABLED with the reason as title; no `models` key → three disabled, claim lit. Persistence `localStorage['gabe:universe:model']` in try/catch, default `claim` (the theme law `:3053`/`:3098`); a stored view the feed lacks falls back to claim with ONE console line.
- **Battery:** FIRE: exactly four `data-v` values in order (index compare, the `t_order` pattern at `run.sh:26-29`); the handler calls `__uniSetModel`; `id="entmodel"` built on all three call-site paths. SILENT: no `data-v="r2"`/`r3`/`r4` anywhere.

### B4 · Delete the R2–R4 machinery, not just the buttons
- **Anchor:** `:3942 window.UNIWIRE={ r1:false, r2:false, r3:false, r4:false };` · `:1658`/`:1678`/`:2167` (`__uniRelHide` gates) · `:1666 __uniDrawBundles(connGroup)` · the R4 branch in `tuneLinkForce` (`UNIWIRE.r4&&l.rel==="renders"&&_SOLEP`).
- **Change:** remove `UNIWIRE`, `_wireSets/_UTILSET/_SOLEP`, `__uniRelHide`, `__uniDrawBundles`, the R4 spring, and the three light-on-demand exemptions that exist only for R-hidden wires. An unreachable toggle that still gates the connector build is the failure mode.
- **Battery:** SILENT (mutation-proven): `window.UNIWIRE` · `__uniDrawBundles` · `_UTILSET` · `UNIWIRE.r4&&l.rel==="renders"` all absent. FIRE: the CAPSULE bundle survives — `run.sh:1156` `rel:"bundle"` + `count:g2.n` (inside `__uniApplyCapsules :3924`, NOT R3) untouched. State the line delta in the commit (800-line CODE budget, report-never-gate).

### B5 · `window.__uniSetModel(v)` — the live re-cluster
- **Anchor:** `:2455` (the per-tick read) · `:1768 function recomputeEX(mode)` · `:1923 window.__uniAssignSplit=` · `:3880 window.__uniApplyCapsules=function(){` · `:2620 function _jrnCollect(){ if(JRN) return JRN;` · `:3878 window.UNICAP={ on:false, threshold:80, open:{} };` · `:2290 UNIVIS.ent[ent]=Object.assign({},_VISDEF)` · `:1239 FE_PAIR[h.id]=h.pair` · `:1242` (twin colour lerp).
- **Change:** in order: (a) resolve the view — absent/unknown → `return false`, stay claim (honest-empty); (b) `H = Object.assign({}, C4.models.homes[v]||{}, (GABE_LEVELS.models||{}).homes?.[v]||{})`, abstain/held sets; for every node in `nodes` AND `_FNNODES`: `n.ent = H[n.id] || n.entClaim; n.modelMark = H[n.id] ? 'moved' : abstain.has(n.id) ? 'abstain' : held.has(n.id) ? 'held' : null` — nothing else on the node touched (no id, no col, no det, no array reassignment); (c) `_ents` = claim entities ∪ the view's live cluster ids minus empties (push the way the FE fold does at `:1240-1246`); (d) `ENT[c]` from the roster `color` (backend ids) / the `:1242` lerp for `fe·d:` twins; `UNIVIS.ent[c]=Object.assign({},_VISDEF)`; `FE_PAIR[c]` from `twin`/`pair`; (e) `UNICAP.open = {}` (ent-keyed — stale ids would reopen phantoms); (f) `recomputeEX(CFG.entLayout)` → `__uniAssignSplit()` → `recomputeSubAnchors()`; (g) `__uniApplyCapsules()` (already idempotent: rebuilds capsule ids, calls buildClusters/updateClusters/rebuildNodes/updateConnectors/buildTransports/__uniFleetRegroup/panelAll); (h) `Graph.d3ReheatSimulation()`; (i) `JRN=null` (spans re-derive), `__uniFleetRender()`, re-render `__uniPView` falling back to `panelAll()` when its ent no longer exists (`:5644`/`:5684`), `applyVis('all')`. MUST NOT CHANGE: `nodes`/`links` arrays and ids (WALK.steps are ids — the tier engine's law at `:4153-4160`); wires; kinds/tiers/feClass/hrole; `n.col`. A walk in progress is PRESERVED.
- **Battery:** FIRE (static): recomputeEX · __uniAssignSplit · recomputeSubAnchors · __uniApplyCapsules · d3ReheatSimulation · `JRN=null` · `UNICAP.open={}` · `_FNNODES` all inside the `__uniSetModel` slice (the `run.sh:1144` slicing pattern). SILENT (static): `Graph.graphData(` · `nodes=` · `n.id=` absent from the slice. Headless: B10.

### B6 · `?model=` deep link, applied BEFORE `?ent=`
- **Anchor:** `:3873 window.__uniApplyDeepLinks=function(){` · `:6125 window.__jrn=q.get("journey"); window.__ent=q…`.
- **Change:** parse `window.__model=q.get("model")`; apply the model FIRST inside `__uniApplyDeepLinks` so `?ent=` resolves against the chosen model's clusters. A URL model NEVER writes localStorage (the `?journey=` law: a shared link must not rewrite the reader's preference). Unknown value → one console line naming the views the feed carries, stays claim.
- **Battery:** FIRE: `q.get("model")` present; the model-apply index precedes the `__jrnDone`/`__entDone` branches (index compare). SILENT: no `localStorage.setItem` in the URL path. Headless: no query → `__uniModel==='claim'`.

### B7 · Hull rim carries cluster KIND; colour stays the entity law
- **Anchor:** `:4955 function makeCluster(members, color, shape, op, level, label){`.
- **Change:** a `dash` flag from the roster kind — `aspect` hulls draw a dashed/stippled rim in the same tint law (a cross-cutting aspect has no locality; a solid hull would claim one). `layer` is REPORT-only this pass (no hull; the legend row says so). An abstained/held piece stays in its claim hull and wears a muted mark on the existing `candidate`/`fixture` channel (`:1251`).
- **Battery:** FIRE (headless): on derived at the functions tier, CLUSTERS has an entry with `ekey==='a:<gate>'` and `dash` set. SILENT: on claim no cluster carries `dash`.

### B8 · Cards — the model row, verdict badges, cluster "what this is"
- **Anchor:** `:5510 function homeEvRow(n){ var e=n.homeEv; if(!e||!e.verdict||e.verdict==="agree") return null;` (reuse its `_hslug` R10 helper) · `:5684 function panelEnt(ent){`.
- **Change:** element card: one row beside `homeEvRow` — `claim <slug> → <view> <cluster>` (moved) · `no witness reaches this piece — it keeps its claim` (abstain) · `shared plumbing (<class>) — held out of every re-home` (held); null on claim. `panelEnt`: on proposed, a verdict badge (FEATURE · SPLIT · MERGE · ASPECT · LAYER) + `why`; for a cluster the claim lacks (`d:`/`a:`), kind + `named_by` + `why` + `purity`. `C.element` card for the new kind (file · fns · routes · "no entity claims this file").
- **Battery:** FIRE: the row function is in every builder list that carries `homeEvRow` (`:5441`, `:5521`, `:5531`, `:5543`); headless: on proposed, an entity the view judges shows a badge whose text ∈ the five verdicts; on claim none. SILENT (R10): no new string contains "orphan".

### B9 · The Sources row per view + the legend MODELS section
- **Anchor:** `:5658 if(window.__uniBudgetHit) w.append(srcRow("entity","scale", …` · `:4637 …'</div><div class="lrcols">'+_connSection()+_planetSection()+'</div>'`.
- **Change:** ONE `srcRow` after the budget row: `entity model — <view> · N moved · M abstained · H held · K new clusters · A aspects · L layers · purity P` (+ `· J cluster(s) folded at this model` when the merge trips `UNICAP.threshold :3894` — say it, never suppress it); on claim / no key: `claim — the only model this feed carries` + the reason. Plus an `elements` Sources row (`N unclaimed files drawn · T ts files unpieced (counted, not drawn)`). A `_modelSection()` as the fifth `lrsec`: rows claim · seeded · derived · proposed · aspect (dashed swatch) · layer (badge, "reported, not drawn") · element (the muted planet), each drawing its ACTUAL swatch (legend-visual law) with an `lrdef` in the user's words (B's six definitions verbatim). Not a connector row (`:4625` rules a view switch is not a wire).
- **Battery:** FIRE (headless): printed counts equal `models.views[v].{moved,abstained,held,new_clusters}`. SILENT: with `GABE_C4.models` deleted the row renders and names the absence. Legend: `_modelSection` concatenated into the body string; seven rows with `lrdef`; no "orphan".

### B10 · The headless switch proof — identity · round trip · journeys · honest-empty
- **Anchor:** `tests/gabe-universe/run.sh:1219 # ── 13. OPTIONAL headless render proof …` (one extra `p.evaluate` block, no second page load; SKIPs loudly without chrome — `:1680`).
- **Change:** capture `before=nodes.map(n=>n.ent)`; `__uniSetModel('seeded')` → `nodes.length` unchanged, every id resolves through NIDS, moved count === `views.seeded.moved`, `_ents` gained each named cluster with `ENT[c]`/`UNIVIS.ent[c]`, an abstained piece's `ent===entClaim`, `sub==='other'` count did not rise; start a walk, switch to derived, `WALK.steps`/`WALK.i` unchanged, `_jrnCollect()` non-empty; `__uniSetModel('claim')` → element-wise equal to `before` (THE mutation proof — break the hold-out or the empty-cluster prune and it fails); delete `GABE_C4.models`, `__uniSetModel('derived')` returns false and `__uniModel==='claim'`; restore.

### B11 · Retire the R2–R4 pins; rewrite the two that survive
- **Anchor:** `tests/gabe-universe/run.sh:1127-1135` (UNIWIRE literal · `__uniRelHide` · R3 bundling · R4 spring) · `:1141-1144` (light-on-demand count ≥3 · the `UNIWIRE[d[0]]` lit-state · the `wv-` slice).
- **Change:** RETIRE `:1127-1135` and `:1141-1142`; REWRITE `:1143` to `b.classList.toggle("on", !!UNICAP.on)` and `:1144` to slice on the CAP button id; KEEP `:1136-1139` reworded to `#entmodel`, and `:1156` untouched. Every retired pin is replaced by a B3/B4 pin so the count never silently drops.

### B12 · Other stations stay CLAIM-ONLY (structural, recorded)
`codebase-graph.html` reads l1/l2 by slug and never `models` (byte-identical, additive key); re-clustering it drags `sim-panel.js` (two consumers) into the pass. The levels lab draws claim; its data carries the fn slice for gabe-map and the universe. Pin: `tests/codebase-graph` asserts no `models` read. Trigger to revisit: a study session asks to simulate a change against a derived feature.

---

## Part C — tools · pulse · drafter · docs

### C1 · The 18th tool — `entity_models` (ONE tool, not a `model=` flag on five)
- **File:** NEW `skills/gabe-map/scripts/tools_wave4.py` + `skills/gabe-map/scripts/tools.py:840-843` (the three-line wave pattern: `import tools_wave3 as _w3` · `TOOLS.extend` · `BY_NAME.update`).
- **Change:** modes on one tool — no args → the CENSUS (four views · counts · `today: claim` · `rule`); `model=` → that view's roster with `kind`/`named_by`/`anchor_table`/`pieces`/`purity` + `abstained` + the coverage number; `entity=<name>` (+`model`) → members capped at `mq.CAP` (cap named) with witnesses; `piece=<file#fn|endpoint:…|fe:…|path>` (via `detect_kind`, `tools.py:269`) → the CROSS-MODEL row `{claim, seeded, derived, proposed, why, mark}`. Reads `c4.models` for census/roster (already loaded — delta-sized) and `levels.models` LAZILY for fn pieces (`mapquery.py:157`); `map_status` never touches levels. Annotations RO; description ≤200 chars. Why one tool: three of four views produce names that are NOT slugs and gabe-map joins on slug everywhere; MCP schemas are deferred (`map-spec.md:34`) so a `model=` argument is invisible until a schema loads while a NAME in the instructions is the discovery surface; 5×4 honest-empty combinations vs 4+2. `map_census` gains NO `models` kind (one surface, not two).
- **Honest-empty:** tri-state in `mq.health_key`'s vocabulary (`mapquery.py:259-263`): `present` · `not_emitted` ("no models block on c4-graph.json — regen with the current generators") · `absent` + the emitter's reason. Unknown model → `MapStop` naming the four. Unknown entity/piece → `{found:false}` + the grep floor.
- **Battery:** `tests/gabe-map/checks.py` — fixture (`:84 def center(`, the `:194-198` levels dict) gains `models` on c4 + levels with one piece `thing` under claim and `other` under derived; `older_map (:228)` stays FREE of both. Cases: `len(names)==18` + `W4={"entity_models"}` (`:325` moves) · census · roster `named_by` · cross-model row claim≠derived · abstained piece says the claim was kept · SILENT `older_map` → `not_emitted`, `isError:false` · `map_status` still never loads levels. MUTATION: flip the fixture's derived value → the cross-model case reddens. ~180 lines, own module (tools.py is 843).

### C2 · `mapquery.Center.entity_models()` — the lazy loader + tri-state helper
- **Anchor:** `mapquery.py:157 def levels(self)` · `:114 _CACHE` · `:263 def health_key(a, key)`.
- **Change:** `(block, state)` — c4 half from the loaded graph, levels half behind the lazy gate; cached on the existing `(path, mtime, size)` key. ~25 lines.

### C3 · `touches` — one pointer line, zero schema change
- **Anchor:** `tools.py:297 def _home_ev(center: mq.Center, piece: str)`.
- **Change:** append `· cross-model: mcp__gabe-map__entity_models piece=<key>` to the returned `note` when the levels/c4 `models` maps hold this key. No block → byte-identical answers. Battery: the Part C case at `checks.py:568` gains the pointer assert with/without the block.

### C4 · `entity_context` — the proposed verdict as one slug-keyed field
- **Anchor:** `tools.py:209 entity_context(slug)`.
- **Change:** `proposed: {verdict, why}` beside the coverage fields when the block is present (a verdict on a SLUG carries no join hazard); absent otherwise. Battery: FIRE with block, SILENT on `older_map`.

### C5 · INSTRUCTIONS — the discovery line
- **Anchor:** `tools.py:805 INSTRUCTIONS = """gabe-map: …` (≈2,150 chars, `map-spec.md:64`).
- **Change:** ONE routing line: `- which entity does this piece belong to under each model (claim · seeded · derived · proposed) → mcp__gabe-map__entity_models (claim IS the registry; the other three are VIEWS — nothing joins on their names)`. ≈2,300 chars. Battery: extend F17 (`checks.py:326-328`), keep `"orphan" not in ins`.

### C6 · map-spec + gabe-map SKILL.md + CLAUDE.md row
- **Anchors:** `map-spec.md:10`, `:35`, `:64`, `:233` (seventeen → eighteen; 2,150 → ~2,300); new §5.10 "Wave 4 — the entity models" (the views, the read contract c4-rosters/levels-pieces, the tri-state, the cap law, the join-key law); `skills/gabe-map/SKILL.md:7 version: 1.2.1` → 1.3.0 + one table row; `CLAUDE.md:93` row. Doctor parity (`suite-doctor.sh:113-123`) catches a missed row.

### C7 · Pulse S9 — two arms; the detector that measures reports
- **Anchor:** `angles.py:305 def s9_entity_shape(root, plan, cfg)` · `:327 parts.append(f"{len(orphans)} orphan domain(s) …")` · `:330` (the aspect phrase).
- **Change:** ARM A (fresh, unchanged computation): detached domains only — copy `orphan domain` → `detached domain` (R10). ARM B (emitted): read `models.rosters.derived[kind=='aspect']` from the committed c4 via `fetch_bridge._center(root)` (`:597` pattern); each row prints its DETECTOR + evidence (`auth is an aspect (gate fan-in: get_auth_context spans 23 domains)`); block absent → `aspects: not_emitted — regen to know` INSIDE the line while arm A still fires; `present:false` → the reason. Fire on ≥1 aspect or ≥1 detached domain. Move: `/gabe-cc-init rank`. The URL co-claim aspect PHRASE retires from the line (measured: gate fan-in 23/24 · 34/41 is the detector; screen co-fetch is not). Decay note in the commit: the evidence-text hash changes, so S9 re-offers twice on suppressed repos (pulse-spec §5.3) — expected.
- **Battery:** `tests/pulse-angles/run.sh:135-149` — keep s9a/s9b/s9c EXACTLY (arm-A regression); add S9-B FIRE (`mkc4 :356` + an emitted gate-fan-in aspect) · S9-B SILENT (empty aspects) · S9-B NOT_EMITTED (assert BOTH `not_emitted` and the arm-A phrase — a half-signal must never read clean) · RETIREMENT REGRESSION (s9a's URL-co-claim `shared` produces NO aspect phrase) · R10 `detached`.

### C8 · `entity_shape.py` — the aspect half STAYS (the hazard item)
- **Anchor:** `entity_shape.py:26-29 ASPECT_COCLAIM_MIN = 3 / ASPECT_SOLE_MAX = 1` · `:78-91` (orphan = no NON-aspect entity claims it) · `:118 def classify_new_routes(new_routes, owned, orphans, …)` · `:141`/`:213` CLI "ORPHAN" copy.
- **Change:** NO computation change — `owned` is defined against `aspect_set` and feeds `classify_new_routes` → `/gabe-review` ENTITY-SHAPE DRIFT + `mcp__gabe-map__entity_shape diff` (`map-spec.md:133-136`); removing it silently changes review verdicts on every project. Add the comment block saying so; the docstring + CLI copy say `detached` (R10) while the JSON key `orphans` stays (a contract three callers read). Battery: `tests/entity-drift` green UNCHANGED (the proof the rewire is reporting-only) + one pulse-angles assert that `--diff` classification is byte-identical.

### C9 · S17 — thresholds unchanged, pointer updated
- **Anchor:** `angles.py:609 if move < 3:` · `:615 (… mcp__gabe-map__map_census kind=homing lists the candidates)`.
- **Change:** pointer → `mcp__gabe-map__entity_models model=seeded` (the seeded view IS the move candidates applied, with destinations); one clause: the shared count is the `fe-homes` input to S9-B. NOT folded into S9 (per-piece MOVE debt vs model SHAPE). Battery: existing FIRE/SILENT + the pointer assert.

### C10 · S18 — proposal debt (ships WITH the drafter; the S16 precedent)
- **Anchor:** `angles.py:570 def _pending_drafts(p)` · `:645-649` ("the REVIEW is owed, not the run") · `:658 SIGNALS`.
- **Change:** `s18_entity_proposals` reads the committed `entities.draft.json`: fires on ≥1 non-FEATURE declared verdict or ≥`THRESHOLDS['entity_candidates']` (2) candidates. Two states: draft `head` == committed c4 `head` → REVIEW owed, move `/gabe-cc-init rank`; head mismatch → RUN owed (STALE), move re-run the drafter. No file → silent; unparseable → 0 never a crash; no center → Unavailable. Trigger count 17→18 in pulse SKILL.md + CLAUDE.md; pulse 1.7.7→1.8.0.
- **Battery:** FIRE current-head SPLIT · FIRE stale-head naming the mismatch · SILENT all-FEATURE no candidates · SILENT no file.

### C11 · pulse-spec + pulse SKILL.md
- **Anchor:** `pulse-spec.md:318` (S9 row) · `:372-397 §5.6` esp. `:388 **No stored artifact.**` → "arm A stores nothing; arm B is exactly as fresh as the committed c4 and says so when the block is missing"; the S18 row; `skills/gabe-pulse/SKILL.md:136-139`.

### C12 · The drafter — `skills/gabe-cc-update/scripts/draft-entities.py`
- **Anchor:** `draft-workflows.py:18-20` (the laws) · `:50 _words` · `:55 draft_name` · `:104 _dedupe_names` · `:265-313 main()` (honest-empty triad + `:300 rep["written"] = changed`).
- **Change:** a PROJECTION of the committed `c4.models` — re-reads nothing, re-derives nothing (one source, one truth; `homes.proposed` is already the station's rendering of the same verdicts, so this file is NEVER loaded by a page — plain JSON, not a `window.*` .js). Home: cc-update beside its sibling (the operator's stated location); OWNERSHIP of acceptance is cc-init (C13) — cc-update's binding table gets the script row + a cross-pointer "accepted via /gabe-cc-init rank". CLI mirrors the sibling: `<root> [--out] [--json] [--model proposed|derived] [--min N]`. NAMING LAW (2026-09-05 ruling): every candidate arrives NAMED and CLASSIFIED — `named_by` from the emitter (domain → table → screen), `_words()` on the segment, `draft_name()`'s verb-set action phrase, `_dedupe_names` for collisions. `coverage.witnessed` + `abstained` are NON-OPTIONAL (31/29/46/29% witness coverage must never read as "69% wrong"). Deterministic (sorted, `head` the only stamp, unchanged input rewrites nothing); honest-empty ×4 (no `center.config.json` · no c4 · unreadable c4 · **no `models` block → "regen with the current generators"**), nothing written, exit 0; exit 1 only on write failure; an all-FEATURE/no-candidate run writes a VALID EMPTY draft so a stale one never lingers. COMMITTED (the `workflows.draft.js` precedent — S18 and the cc-init lens read it across sessions; the `head` comparison keeps it honest). Never touches `center.config.json`.
- **Battery:** NEW `tests/draft-entities/run.sh` (copy `tests/workflow-drafts/run.sh:1-40` structure). FIRE: three candidates, one per `named_by`; declared SPLIT + MERGE with evidence. SILENT: all-FEATURE → valid empty draft. HONEST-EMPTY ×4 each printing its reason. DETERMINISM: second run byte-identical, "unchanged". ABSTAIN: a witnessless cluster lands in `abstained`, never `candidates`; `coverage.witnessed` present. MUTATION: make the drafter derive a verdict locally → the projection-equality assert reddens; flip the fixture's anchor table → the fallback name changes. R10: no "orphan". cc-update 1.7.7→1.8.0.

### C13 · cc-init `rank` — the ENTITY-MODEL THIRD LENS (advisory)
- **Anchor:** `skills/gabe-cc-init/references/adopt-spec.md:136 2b. **The URL-domain SECOND LENS** … :143-145` (the exact template) · `:151` (re-running `rank` APPENDS, never re-ranks approved rows) · `SKILL.md:12`, `:27`.
- **Change:** step 2c: run `~/.claude/skills/gabe-cc-update/scripts/draft-entities.py .` (or read a current draft); present per-declared verdicts, named candidates with `named_by`, and the coverage number beside the ranking and the URL lens; ADVISES, the operator rules. Acceptance = ONE edit (`entities.<slug>` in `center.config.json`) then `rank` again. Two honest-skips, one line each: no draft → run the drafter; draft `head` ≠ committed c4 `head` → STALE, re-run before trusting (the frozen-example trap). cc-init has no scripts dir and gains none; add a three-row bindings table naming the cc-update script path so a missing script is an E6 STOP. cc-init 1.3.2→1.4.0.

### C14 · Docs — CLAUDE.md · design record · shell README · Part C pointer
- CLAUDE.md rows: gabe-map 1.3.0 (EIGHTEEN tools + `entity_models`) · gabe-pulse 1.8.0 (S9 two-armed — the "nothing stored to go stale" clause corrected; S18) · gabe-cc-update 1.8.0 (drafter) · gabe-cc-init 1.4.0 (third lens); ONE new Conventions bullet: the four models, where each is emitted, which surface reads which, the join-key law in one sentence, the element census bound. NEW `docs/design/entity-models/README.md`: the operator's decision, the MEASURED table verbatim (purity 0.646/0.654/0.660/0.576 · hub ablation 0.857/0.908/0.727/0.704 removing 41/16/5/109 · witness coverage 31/29/46/29% backend, 92/67/87/57% with FE · free propagation 34–42% · gate fan-in 23/24, 34/41 · screen co-fetch NOT · triads 6/8·4/5·2/4·0/9 · gastify 33 routes/254 fns/12 tables unclaimed · graft edges all inferred), the abstention law, the derived purity 0.910/0.898/0.893, every cap with its kill condition, the deferred items (Depends-modularity · layer hulls · drawn fe-shared aspects · other stations · TS unpieced minting) with triggers, and the KILL condition for the derived view: zero candidates accepted across 3 drafts on both twins ⇒ the pill is a picture, not a proposal — say so on the design record, do not delete. `templates/center/shell/README.md` (beside `:43`): the pill reads `window.GABE_C4.models` (+ `GABE_LEVELS.models` for functions), switches a VIEW, never derives; `entities.draft.json` is loaded by NO page (pin: no `fetch(` of it in either copy). One pointer line under `legend-and-tools-plan.md:495` Part C.

---

## Invariants and proofs
1. **Claim view byte-identical when stripped:** `c4` minus `models` + `stats.models` == today's bytes (tests/arch-graph); `levels` minus `models` == today's bytes (tests/levels); with the element census ABSENT, l1/l2/cross_edges/fe are byte-identical — with it present, ONLY `l2.__unclaimed__` gains `element:` nodes and `stats.elements` appears (two separate pins).
2. **Honest-empty ladder:** claim always present · seeded needs levels+homing · derived needs ≥1 anchored atom · proposed needs derived; every absent view `{present:false, reason}`; every consumer (station pill, Sources row, `entity_models`, S9-B, S18, the drafter, the cc-init lens) prints the state word — never a silent clean, never an empty roster that reads as coverage.
3. **Determinism:** sorted inputs, sorted ties, no LPA, no RNG, no wallclock; `models.head == graph.head == levels.models.head`; two regens byte-identical.
4. **Abstain where no witness reaches:** an unanchored atom, a tier-inconsistent seed and a hub keep the claim and are LISTED (`abstain`/`held`), and the counts print on the Sources row and in the draft's `coverage`.
5. **`claim` is the join key:** no tool/hook/generator joins on a view name; `entity_context`/`owner_of`/`find` never return a `d:`/`a:` name where a slug is expected.
6. **Station identity:** `nodes`/`links` arrays and every id unchanged across a switch; the claim round trip is element-wise equal; a walk survives a switch.
7. **R10 · two-file law · mutation proof for every new pin · 800-line CODE budget stated in commits (`_a3_models.py` targeted <800; `_a3_graph.py` grows by the element mint only).**

## Build order — phases, each ending in a review; [H] = heavy, serial, nothing else running
- **Phase 0 — prerequisites (census + element nodes + byte pins).** A1 · A2 · the `tests/entity-models` skeleton · `tests/arch-graph` byte-identity pins. Batteries: entity-models, arch-graph (light). Review: R0 (dry-run `element_census` on a COPY of gastify's tree — 58/254/31 expected — numbers into the commit).
- **Phase 1 — the emitter.** A3 · A4 · A5 · A6 · A7 · A8 · A9. Batteries: entity-models (hermetic synthetic archmap+c4+levels), levels, arch-graph, center (incl. `propagate.sh --check` classifying `_a3_models.py` as `req_gen`) — all light. Review: R1 (purity/abstain/hub numbers on a COPY of each twin's committed maps vs the measured 0.910/0.898/0.893; the naming fixture's real-data check for `chat/delete-all-chat-sessions`-class names).
- **Phase 2 — the station (both copies).** B1 → B2 → B3 → B4 → B5 → B6 → B7 → B8 → B9 → B11 → B10. Battery: `tests/gabe-universe` **[H] ≈1.5 min** (static pins + the headless switch proof) — alone. Review: R2 (a real switch on the example feed: identity, round trip, walk preserved, fold-on-merge said).
- **Phase 3 — tools + pulse.** C1 · C2 · C3 · C4 · C5 · C6 · C7 · C8 · C9 · C11. Batteries: gabe-map, pulse-angles, entity-drift (must be green UNCHANGED) — light. Review: R3 (the join-key law probed: feed a `d:` name to `entity_context` and get `found:false`, not a crash).
- **Phase 4 — the drafter + S18 + the cc-init lens.** C12 · C10 · C13. Batteries: draft-entities (new), pulse-angles — light. Review: R4 (a draft on a COPY of gustify's c4: `dish_history_events` arrives named, coverage printed, head stamped).
- **Phase 5 — docs · install · doctor · estates.** C14 → `./install.sh` (≈5 s) → `scripts/suite-doctor.sh` **[H] 2–4 min** (must be CLEAN: version/count parity, all batteries) → estates ONE AT A TIME: `regen-example.sh` **[H]** (fill-example.py, then `--check` byte-clean — remember it REVERTS uncommitted example edits: commit the station first) → tiers 0–3 via `bootstrap_center.sh` + `refresh_center.sh regen` **[H] ×4** → twins via `propagate.sh --check` then `propagate.sh` + regen **[H] ×2** → drafter run on each estate (light). Per estate record in the commit: seeded moved/held/abstained · derived features/anchored/abstained/purity · proposed verdict tally · candidates · elements. Final review: R5 (the numbers vs the measured table; onyx's `abstained 172` printed beside `anchored 327` on its Sources row; `?model=derived&ent=<slug>` on tier3 boots folded and resolves).

## What stays out (with triggers)
Depends-modularity aspect detector (trigger: an aspect the three detectors miss) · layer hulls + moving fe-shared pieces into an aspect hull (trigger: a reader asks to SEE the shared plumbing as a field) · minting TS unpieced files (trigger: a config/test file the drafter needs to name) · re-clustering `codebase-graph.html`/`sim-panel.js` (trigger: a change simulated against a derived feature) · the `homing: usage-first` config switch (Part C's own trigger: ≥5 hand-accepted moves on a twin) · a `model=` argument on existing tools (never: the join-key hazard).